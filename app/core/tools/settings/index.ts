import { windowBounds } from './window-bounds'
import { connSettings } from './connection-settings'
import { appSettings } from './app-settings'

export * from './create-settings'

export const settings = {
  windowBounds,
  connSettings,
  appSettings,
}
